#include <bits/stdc++.h>
using namespace std;
long long int big = 1e9 + 7;
void solver(int n,vector<int>v1){

}
int main(){
    int t;cin>>t;
    while(t--){
        int n;cin>>n;
        vector<int> arr(n);
        for(int i=0;i<n;i++) cin>>arr[i];
        long long int sumy=0;
        for(int i=0;i<n;i++){
            sumy+=arr[i];
        }
        long long int suma1=0;
        bool ans=true;
        for(int i=0;i<n-1;i++){
            suma1+=arr[i];
            if(suma1>=sumy){
                ans=false;break;
            }
            if(suma1 < 0) suma1=0;
        }
        long long int suma2 = 0;
        for(int i=1;i<n;i++){
            suma2+=arr[i];
            if(suma2>=sumy){
                ans=false;break;
            }
            if(suma2 < 0) suma2=0;
        }
        if(ans) cout<<"YES"<<endl;
        else cout<<"NO"<<endl;
    }
}
